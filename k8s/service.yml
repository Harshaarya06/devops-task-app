apiVersion: v1
kind: Service

metadata:
  name: devops-task-app-service

spec:
  selector:
    app: devops-task-app

  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000

  type: ClusterIP